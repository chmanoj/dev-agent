[project]
name = "streamlit-hello-world"
version = "0.1.0"
description = "A multi-page Streamlit hello world application"
requires-python = ">=3.8"
dependencies = [
    "streamlit>=1.28.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
]